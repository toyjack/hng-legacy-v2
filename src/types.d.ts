export type Record = {
  entry: string;
  variants: string;
  id: string;
  djt: string;
  dkw: string;
  ucs: string;
};

export type Glyph = {
  id: string;
  glyph_num: string;
  glyphs: string;
  occerrences: string;
  book_id: string;
};